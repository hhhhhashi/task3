<h1>User info</h1>

<%= form_with model: @user do |f| %>

  <h4>name</h4>
  <%= f.text_field :title %>
  <!-- 追加部分 -->
  <h4>Image</h4>
  <%= f.file_field :image, accept: "image/*" %>

  <h4>Introduction</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Updete User' %>

<% end %>
